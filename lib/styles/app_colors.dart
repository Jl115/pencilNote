import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF4B0082); // Purple
  static const Color secondaryColor = Color(0xFF00BCD4); // Cyan
  static const Color backgroundColor = Color(0xFFFFFACD); // Light Yellow
  static const Color surfaceColor = Color(0xFFFFFFFF); // White
  static const Color errorColor = Color.fromARGB(255, 238, 188, 128); // Red
  static const Color onPrimaryColor =
      Color(0xFFFFFFFF); // White for text on primary
  static const Color onSecondaryColor =
      Color(0xFF000000); // Black for text on secondary
  static const Color onBackgroundColor =
      Color(0xFF000000); // Black for text on background
  static const Color onSurfaceColor =
      Color(0xFF000000); // Black for text on surface
  static const Color onErrorColor =
      Color(0xFFFFFFFF); // White for text on error
}
